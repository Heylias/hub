<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">Hubfiction Admin</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard_index') }}"><i class="fas fa-chart-line"></i> Dashboard</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_fictions_index') }}"><i class="fas fa-book-open"></i> Fanfictions</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_tags_index') }}"><i class="fas fa-tags"></i> Tags</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments_index') }}"><i class="fas fa-comments"></i> Comments</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users_index') }}"><i class="fas fa-users"></i> Users</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_chapters_index') }}"><i class="fas fa-book"></i> Chapters</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_genres_index') }}"><i class="fas fa-tags"></i> Genres</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_languages_index') }}"><i class="fas fa-language"></i> Languages</a>
            </li>

        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('homepage') }}" target="_blank" class="nav-link"><i class="fas fa-eye"></i> Home</a>
            </li>
          {% if app.user %}
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                    {% if app.user.userImage %}
                        <img src="/uploads/{{ app.user.userImage }}" alt="{{ app.user.pseudonym }}" class="avatar avatar-mini">
                    {% else %}
                        <img src="http://placehold.it/64x64" alt="{{ app.user.pseudonym }}" class="avatar avatar-mini">
                    {% endif %}
                    {{ app.user.pseudonym }}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Log out</a>
                </div>
            </li>
          {% endif %}
        </ul>
    </div>
</nav>
   {# {{ dump(app.flashes) }} #}
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{message | raw}}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}