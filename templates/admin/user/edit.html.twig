{% extends 'admin/base.html.twig' %}

{% block title %}Editing user {{user.pseudonym}}{% endblock %}

{% form_theme myForm _self %}

{% block body %}
    <div class="container">
        <h1>Editing the user: {{user.pseudonym}}</h1>
        <a href="{{path('admin_users_index')}}" class="btn btn-secondary my-3">Back</a>
        {{form_start(myForm)}}
        <div class="row">
            <div class="col">
                <div class="card bg-light">
                    <div class="card-header">
                        <h4 class="card-title">Editing the user's info?</h4>
                    </div>
                    <div class="card-body">
                        {{form_widget(myForm)}}
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-check"></i>
                            Save edits
                        </button>
                        <a href="{{ path('admin_users_index') }}" class="btn btn-secondary">Back</a>
                    </div>
                </div>
            </div>
        </div>
        {{form_end(myForm)}}
    </div>
{% endblock %}